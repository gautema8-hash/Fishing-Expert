# BOSS 金龙姿态与游泳动画优化说明

修改日期：2026-09-13
涉及文件：
- `frontend/js/config/fishConfig.js`
- `frontend/js/entities/Boss.js`

目标：新 BOSS 图（2048×2048，优雅 S 形游姿）就位后，让金龙游动更有流动感、姿态更优美，同时保留全部既有玩法。

---

## 1. fishConfig.js —— imageScale 调整

`dragonking.imageScale`：`3.0` → **`2.8`**

- 略微缩小贴图，确保完整 S 形龙身（从头到尾）完整落在屏幕内，不会因过大而头尾被裁出画面。
- 渲染时比例计算保持 `imgH = imgW * (naturalHeight / naturalWidth)`，未改动。

## 2. Boss.js —— 新增状态字段

构造函数与 `reset()` 中新增：

| 字段 | 作用 |
|---|---|
| `_sparkles` | 龙鳞闪光对象池，同屏硬上限 15 个（性能保护） |
| `_dirTimer` | 漫游方向随机调整倒计时（初始 15s） |
| `_dirBias` | 临时方向偏置，注入后随时间缓慢衰减 |

## 3. 游泳摆动动画增强（`_renderBossImage`）

| 参数 | 旧值 | 新值 |
|---|---|---|
| 整体摇摆 `sway` | `sin(time*2)*0.04` | `sin(time*1.5)*0.08`（幅度翻倍、频率放缓，更舒展） |
| 呼吸缩放 `breath` | `1+sin(time*1.5)*0.03` | `1+sin(time*1.2)*0.04` |
| 蜿蜒位移 | 无 | 沿垂直游进方向叠加 `±size*0.02~0.03` 的同频微位移，配合旋转形成"扭动"观感 |

`render()` 的 `translate` 增加整体缓慢上下浮动：

```
bobY = sin(time*0.8) * 15
translate(x, y + bobY)
```

- 纯视觉层偏移，不影响碰撞盒与边界判定（边界仍按 `this.y` 钳制）。

## 4. 龙鳞闪光粒子（对象池重写）

从"每帧 15% 概率画一个临时光点"改为**带生命周期的对象池**：

- 生成概率：15% → **25%**；同屏上限 **15** 个。
- 光点尺寸：3–8px → **3–12px**。
- 位置偏向龙身区域：x ∈ [-size*1.0, size*0.8]，y ∈ [-size*0.4, size*0.4]。
- 颜色按概率混合：70% 金 `#FFD700` / 15% 白金 `#FFFFCC` / 15% 橙金 `#FFA500`。
- 每个粒子带 `life(0.4–0.9s)` 与 `birth`，透明度按 `sin(π·age/life)` 淡入淡出，过期自动回收，无 GC 压力。
- 改在 `drawImage` **之后**以 `lighter` 叠加，光点真正落在鳞片上可见。

## 5. 飘动龙须（图片模式新增）

- 从龙头位置（`x≈size*0.62, y=±size*0.08`）向后（-x）拖出两段贝塞尔曲线。
- 控制点随 `time` 上下波动，龙须呈飘逸摆动；半透明金色（0.45 alpha），须尖 10px 发光点（`lighter`）。
- BOSS 掉头（angle≈π）时随整体旋转自动镜像。

## 6. 水中光影（caustics）

- 图片之上叠加一个移动的淡蓝白光斑：
  - `lighter` 合成，峰值透明度约 0.08；
  - 中心位置 `x=sin(time*0.5)*size*0.3`、`y=cos(time*0.7)*size*0.15`；
  - 半径 `size*0.9`，渐变到透明。
- 缓慢扫过龙身，模拟水下焦散光斑流动。

## 7. 游动路径优化（`update`）

- 摆角更平滑：`sin(pathTime*0.3)*0.3` → **`sin(pathTime*0.25)*0.25`**。
- 垂直波浪幅度加大：`sin(pathTime*0.8)*20*dt` → **`sin(pathTime*0.6)*30*dt`**（频率放缓、幅度加大，升降更明显）。
- 每 15–20 秒随机注入一次方向偏置 `_dirBias ∈ [-0.4, 0.4]`，随后按 `0.5/s` 衰减，产生偶发的大转向而不破坏边界巡游逻辑。
- 冲撞（telegraphing/charging）、边界钳制、血条、受击闪烁、死亡动画、出场预警、程序化 fallback 全部未改动。

## 8. 保留的既有功能（未触碰）

- 出场预警 `warning` 状态全屏金光
- 冲撞技能 `telegraphing`（红色预警圈+方向线）/ `charging`（3 倍速+拖尾事件）
- `eventBus` 事件 `boss:charge_start` / `boss:charge_trail`
- 血条 `_renderHealthBar`、受击 `_hitFlash`、死亡淡出
- 图片加载失败时的程序化骨骼龙 fallback
- 公共接口与 `Fish` 继承机制

## 9. 验证

- `node --check` 两个文件均通过（语法 0 错误）。
- 粒子数有硬上限（15），每帧仅创建/回收少量对象，`lighter` 绘制调用有限，目标 FPS ≥ 55。
- 建议浏览器刷新（带 `?bust=` 参数强刷）后观察：金龙应完整入画、左右摇摆+上下浮动、鳞片金点闪烁、龙须后飘、光斑扫身。
